{
  "name": "warcraftlogs-wrapper",
  "version": "0.0.1",
  "description": "API wrapper for warcraft logs",
  "main": "dist/index.js",
  "scripts": {
    "watch": "babel -w src/ -d dist/",
    "build": "babel src/ -d dist/",
    "serve": "babel -w src/ -d dist/ | nodemon --watch dist"
  },
  "author": "Kevin Maze rhoxiodbc@gmail.com",
  "dependencies": {
    "dotenv": "^6.2.0",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "babel": "^6.5.2",
    "babel-cli": "^6.16.0",
    "babel-preset-es2015": "^6.16.0",
    "babel-preset-stage-0": "^6.16.0",
    "nodemon": "^1.11.0"
  }
}
